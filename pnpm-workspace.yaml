packages:
  - "apps/*"
  - "packages/*"

# pnpm 10 blocks dependency build scripts by default. These packages need to
# compile native binaries during install.
onlyBuiltDependencies:
  - "@prisma/engines"
  - "@tailwindcss/oxide"
  - esbuild
  - prisma
  - sharp
  - unrs-resolver
